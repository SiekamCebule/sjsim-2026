# @sjsim/core

Shared core for Sj.Sim Predazzo 2026 Edition. Domain logic and interfaces will be added here according to REQUIREMENTS.md.

Currently provides a minimal dependency-injection container so the app and infra layers can register and resolve services (e.g. future persistence adapters) without depending on concrete implementations.
