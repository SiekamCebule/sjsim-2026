# Dependencies
node_modules
.pnpm-store

# Build / cache
dist
out
build
packages/*/dist
packages/*/node_modules
*.tsbuildinfo

# Lockfiles (uncomment if you want to ignore; many projects commit pnpm-lock.yaml)
# *.lock
# pnpm-lock.yaml

# OS / editor
.DS_Store
*.log
Thumbs.db

# Optional: Cursor IDE rules (uncomment if you don't want to commit them)
# .cursor

fakeNames.csv